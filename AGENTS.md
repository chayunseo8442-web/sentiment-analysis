# AGENTS.md — 감성 분석 웹앱 작업 규칙

## 1. 문서의 목적

이 파일은 AI 에이전트(코딩 에이전트 포함)가 이 프로젝트를 구현·수정할 때 따라야 하는 공통 작업 규칙이다.

제품의 기능과 완료 조건은 `PRD.md`를 우선한다. 세부 구현은 `docs/` 문서를 따른다.
문서끼리 충돌하면 다음 우선순위를 적용한다.

1. 사용자 명시 요구사항
2. `PRD.md`
3. `AGENTS.md`
4. `docs/`의 세부 구현 문서
5. 에이전트의 일반적인 추측

추측으로 요구사항을 추가하지 않는다.

---

## 2. 제품 범위

### 반드시 구현해야 하는 기능

- 사용자가 텍스트를 입력할 수 있다.
- 사용자가 분석을 요청하면 서버가 OpenAI API에 감성 분석을 요청한다.
- 결과는 `긍정`, `부정`, `중립` 중 정확히 하나로 표시한다.
- 결과에 신뢰도 백분율을 표시한다.
- 분석 이유를 사람이 이해할 수 있는 한국어로 표시한다.
- 실패 상황에서는 사용자가 이해할 수 있는 오류 메시지를 표시한다.
- 분석 기록은 Supabase에 저장한다.
- UI는 첨부된 참고 이미지의 방향성을 따른다:
  - 흰색 중심의 넓은 여백
  - 상단 얇은 내비게이션
  - 큰 헤드라인
  - 검정/짙은 남색의 기본 텍스트
  - 핑크 계열의 강조색
  - pill 형태의 버튼/탭/필터
  - 둥근 모서리의 입력 영역과 카드
  - 데스크톱에서 2열 hero 구조
- 프론트엔드는 HTML/CSS/JavaScript, 백엔드는 Node.js, AI는 OpenAI API, DB는 Supabase, 배포는 Vercel을 기준으로 한다.

### 구현하지 않는 기능

명시적으로 요구되지 않은 아래 기능은 MVP에 추가하지 않는다.

- 로그인/회원가입
- 관리자 화면
- 다국어
- 파일 업로드
- 음성 분석
- 이미지 감성 분석
- 대화형 챗봇
- 사용자별 개인화
- 결제
- 소셜 공유
- 실시간 스트리밍 분석
- 별도의 자체 ML 모델 학습
- 감성 결과를 의료/법률/채용 등의 고위험 판단에 사용하는 기능

추가 기능이 필요하면 먼저 요구사항을 문서에 추가하고 범위를 확정한다.

---

## 3. AI 에이전트의 추측 방지 규칙

### 금지

- 문서에 없는 필드를 임의로 추가하여 API 계약을 바꾸지 않는다.
- “아마 필요할 것 같다”는 이유로 로그인, Redis, 별도 큐, 관리자 기능 등을 추가하지 않는다.
- OpenAI API 키를 브라우저 코드에 넣지 않는다.
- 모델이 반환한 값을 검증 없이 화면에 출력하지 않는다.
- 감성 `confidence`를 통계적으로 보장된 확률이라고 표현하지 않는다.
- 사용자가 입력하지 않은 문장을 임의로 보완해서 분석하지 않는다.
- 분석 실패를 성공으로 위장하지 않는다.
- DB 저장 실패를 조용히 무시하지 않는다.
- 테스트가 실패했는데 완료했다고 보고하지 않는다.

### 반드시 확인할 것

작업 전 다음 문서를 읽는다.

1. `PRD.md`
2. `docs/01-architecture.md`
3. `docs/02-ui-spec.md`
4. `docs/03-api-and-openai.md`
5. `docs/04-supabase.md`
6. `docs/05-validation-errors-security.md`
7. `docs/06-vercel-deploy-operations.md`

작업 후에는 PRD의 완료기준과 검증 기준을 하나씩 확인한다.

---

## 4. 구현 규칙

### Front-End

- HTML/CSS/JS를 기본으로 하며 프레임워크 도입은 별도 승인 없이는 하지 않는다.
- API 호출은 브라우저에서 `/api/analyze`만 호출한다.
- OpenAI와 Supabase의 비밀키는 브라우저에 노출하지 않는다.
- 버튼을 누르면 중복 요청이 발생하지 않도록 요청 중 상태를 표시한다.
- 입력이 비어 있으면 API를 호출하지 않는다.
- 결과 영역은 분석 전에는 자연스럽게 숨기거나 비활성 상태로 둔다.
- 오류는 입력창 근처와 결과 영역에서 사용자가 이해할 수 있게 보여준다.
- 모바일에서도 입력과 결과를 사용할 수 있어야 한다.

### Back-End

- 요청 형식과 응답 형식을 고정한다.
- 서버에서 입력 길이와 형식을 먼저 검증한다.
- OpenAI 응답은 구조화된 데이터로 받고 서버에서 다시 검증한다.
- 허용 감성은 `positive`, `negative`, `neutral` 세 값뿐이다.
- confidence는 0~100의 숫자여야 한다.
- reason은 비어 있지 않은 문자열이어야 한다.
- 검증 실패 시 성공 응답을 보내지 않는다.

### Database

- Supabase에는 분석에 필요한 최소 정보만 저장한다.
- API 키와 서비스 비밀정보는 DB에 저장하지 않는다.
- 스키마 변경은 `docs/04-supabase.md`와 함께 갱신한다.

---

## 5. 코드 품질

- 함수는 한 가지 역할을 수행하도록 작성한다.
- 오류 처리는 명시적으로 작성한다.
- 사용자에게 표시되는 문구와 내부 로그를 구분한다.
- 비밀정보를 로그에 출력하지 않는다.
- 주석은 “무엇을 하는지”보다 “왜 필요한지”를 설명할 때 사용한다.
- 사용하지 않는 코드와 의존성은 추가하지 않는다.

---

## 6. 완료 보고 규칙

에이전트는 작업 완료 시 다음을 반드시 보고한다.

- 변경한 파일
- 구현한 기능
- 실행한 검증
- 통과/실패한 항목
- 아직 미완료인 항목
- 환경변수 또는 외부 서비스 설정이 필요한 항목

검증하지 않은 항목을 “완료”라고 표현하지 않는다.
