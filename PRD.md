# PRD — 텍스트 감성 분석 웹앱

## 1. 제품 한 줄 설명

사용자가 문장을 입력하면 AI가 문장의 감성을 `긍정 / 부정 / 중립`으로 분류하고, 신뢰도 백분율과 판단 이유를 쉽게 보여주는 웹앱이다.

---

## 2. 문제 정의

사용자는 긴 글을 읽고 직접 감정을 판단하기보다, 한 화면에서 텍스트의 전반적인 감성을 빠르게 확인하고 싶다.

따라서 MVP는 다음 세 가지를 명확하게 제공한다.

1. 감성 결과
2. 신뢰도 백분율
3. 왜 그렇게 판단했는지에 대한 설명

---

## 3. 목표 사용자

- AI/개발 지식이 많지 않은 일반 사용자
- 고객 리뷰나 의견을 빠르게 확인하려는 사용자
- 간단한 문장의 분위기를 확인하려는 사용자

---

## 4. 핵심 사용자 흐름

1. 사용자가 웹페이지를 연다.
2. 텍스트 입력창에 문장을 입력한다.
3. `감성 분석하기` 버튼을 누른다.
4. 서버가 입력값을 검증한다.
5. 서버가 OpenAI API에 분석을 요청한다.
6. 서버가 AI 응답의 형식을 검증한다.
7. 결과를 Supabase에 저장한다.
8. 브라우저에 감성, 신뢰도, 분석 이유를 표시한다.
9. 오류가 발생하면 사용자가 다음 행동을 알 수 있는 메시지를 표시한다.

---

## 5. 기능 요구사항

### FR-01 텍스트 입력

- 사용자는 텍스트를 입력할 수 있어야 한다.
- 빈 문자열은 분석할 수 없다.
- MVP 권장 입력 제한은 5,000자 이하이다.
- 공백만 입력한 경우도 빈 입력으로 처리한다.

### FR-02 감성 분석

감성은 다음 세 가지 중 하나여야 한다.

- 긍정 (`positive`)
- 부정 (`negative`)
- 중립 (`neutral`)

AI가 다른 값을 반환하면 서버는 오류로 처리한다.

### FR-03 신뢰도

- 결과에 `0~100%` 범위의 값을 표시한다.
- 화면에는 사용자가 이해하기 쉬운 형태로 표시한다. 예: `신뢰도 87%`.
- 이 값은 모델이 해당 분류를 얼마나 확신하는지 나타내는 모델 출력값이며, 통계적으로 보정된 확률 또는 정답률을 의미하지 않는다.

### FR-04 분석 이유

- AI는 결과의 근거를 짧고 이해하기 쉬운 한국어로 제공한다.
- 이유는 입력 문장에 실제로 나타난 표현이나 의미를 바탕으로 작성한다.
- 사용자가 입력하지 않은 사실을 근거로 만들지 않는다.
- MVP에서는 1~3개의 짧은 문장으로 제한한다.

### FR-05 오류 메시지

다음 상황을 사용자에게 구분해서 보여준다.

- 입력값 오류
- 서버 오류
- AI 서비스 오류
- AI 결과 형식 오류
- DB 저장 오류
- 요청 시간 초과 또는 일시적 네트워크 오류

오류 메시지는 기술 용어보다 “무엇이 문제이고 무엇을 하면 되는지”를 우선한다.

예:
- `분석할 문장을 입력해 주세요.`
- `문장이 너무 깁니다. 5,000자 이하로 입력해 주세요.`
- `분석 서버에 문제가 있습니다. 잠시 후 다시 시도해 주세요.`

---

## 6. UI/UX 요구사항

첨부된 참고 이미지의 시각적 방향을 사용한다. 특정 브랜드의 로고나 문구를 복제하는 것이 아니라 레이아웃과 분위기를 참고한다.

### 참고 방향

- 전체 배경: 흰색
- 넓은 좌우 여백
- 상단: 작은 내비게이션
- 메인 hero: 왼쪽 텍스트, 오른쪽 시각 카드
- 큰 제목과 핑크 강조 문구
- 둥근 pill 버튼
- 넓은 검색/입력 바
- 카드형 콘텐츠
- 검정 또는 짙은 남색 텍스트
- 핑크 계열을 주요 CTA/상태 강조색으로 사용
- 결과 카드도 둥근 모서리와 충분한 여백 사용

### 제품 화면 구성

1. Header
2. Hero
   - 작은 category pill
   - 큰 제목
   - 설명
   - 텍스트 입력 영역
   - 분석 버튼
3. Result Card
   - 감성 배지
   - 신뢰도
   - 분석 이유
4. 하단의 간단한 안내/주의사항

---

## 7. 비기능 요구사항

### 보안

- OpenAI API 키는 서버에서만 사용한다.
- Supabase service role key도 서버에서만 사용한다.
- 브라우저 번들에 비밀 키가 들어가면 안 된다.
- 사용자 입력을 HTML로 직접 삽입하지 않는다.

### 성능

- 정상적인 짧은 텍스트 분석 결과를 합리적인 시간 내 표시한다.
- 요청 중에는 버튼을 비활성화한다.
- 타임아웃 또는 API 장애 시 무한 로딩 상태가 되지 않는다.

### 접근성

- 입력창에 label 또는 동등한 접근성 이름을 제공한다.
- 키보드만으로 분석을 실행할 수 있어야 한다.
- 결과 변화는 스크린리더가 인지할 수 있도록 구성한다.
- 색상만으로 감성을 구분하지 않는다.

---

## 8. MVP 범위 밖

- 회원가입/로그인
- 분석 결과 검색/필터
- 사용자별 대시보드
- CSV/PDF 내보내기
- 문서 업로드
- 다중 문서 일괄 분석
- 감정 세분화(기쁨/분노/슬픔 등)
- 감성 모델 비교
- 자체 모델 학습
- 결제/구독

---

## 9. 완료기준(Definition of Done)

다음 조건을 모두 만족해야 MVP 완료로 본다.

### 기능

- [ ] 텍스트 입력 가능
- [ ] 빈 입력 차단
- [ ] 5,000자 초과 입력 차단
- [ ] 긍정/부정/중립 중 하나 표시
- [ ] 0~100% 신뢰도 표시
- [ ] 분석 이유 표시
- [ ] 오류 메시지 표시
- [ ] 분석 기록이 Supabase에 저장됨

### 보안

- [ ] 브라우저에서 OpenAI API를 직접 호출하지 않음
- [ ] 비밀 키가 Git 저장소/클라이언트 코드에 없음
- [ ] 서버가 AI 응답을 검증함

### UI

- [ ] 참고 이미지와 같은 “넓은 여백 + hero + 둥근 카드 + 핑크 강조” 방향
- [ ] 모바일에서도 사용 가능
- [ ] 로딩/성공/오류 상태가 명확함

### 배포

- [ ] Vercel에서 실제 분석 요청이 성공함
- [ ] 운영 환경변수가 설정됨
- [ ] 기본적인 브라우저 테스트 완료

---

## 10. 검증 기준

### 정상 케이스

입력 예시:
- `오늘 정말 좋은 서비스를 받았어요.`
  - 기대: 긍정
- `배송이 너무 늦고 응대도 불친절했습니다.`
  - 기대: 부정
- `제품이 오늘 도착했습니다.`
  - 기대: 중립

정답 감성만 고정하지 않고, 실제 모델 응답을 기준으로 핵심 속성의 유효성을 검증한다. 예시 문장의 기대 결과는 회귀 테스트의 기준점으로 사용한다.

### 오류 케이스

- 빈 문자열
- 공백만 입력
- 5,001자 이상
- 서버 오류
- OpenAI API 오류
- 잘못된 AI JSON
- 잘못된 sentiment 값
- confidence가 범위를 벗어난 경우
- Supabase 저장 실패

### 완료 판정

모든 필수 기능과 오류 케이스가 통과하고, 보안 규칙을 위반하지 않아야 한다.
